#!/bin/bash
# Extension: Walker GTK4 Theme Reloader

# Flush Walker's background cache completely to apply the new CSS matrix
pkill -f walker
walker --gdbus --quit >/dev/null 2>&1

# Spin the daemon back up in background service mode
walker --service &
