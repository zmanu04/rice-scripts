#!/bin/bash
# Extension: Walker GTK4 Theme Reloader

# Walker automatically reloads style.css variations when it catches a file modification event.
# We touch its configuration folder to instantly force a cache update.
if pgrep -x "walker" > /dev/null; then
    touch ~/.config/walker/style.css
fi
